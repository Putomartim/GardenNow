import { RotatingText as BaseRotatingText, type RotatingTextProps } from "@/components/rotating-text";

const RotatingText = (props: RotatingTextProps) => <BaseRotatingText {...props} />;

export type { RotatingTextProps };
export default RotatingText;
